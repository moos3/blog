box: wercker/default
build:
  steps:
    - arjen/hugo-build@1.6.1:
        version: "0.14"
        theme: huge-creative-theme
deploy:    
  steps:        
    - step-git-push:
        gh_oauth: $GIT_TOKEN
        gh_pages: True
        gh_pages_domain: blog.guthnur.net
        basedir: build
